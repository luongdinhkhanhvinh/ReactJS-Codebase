import { makeStyles, createStyles } from '@material-ui/core';

export default makeStyles(() =>
  createStyles({
    logo: {
      cursor: 'pointer',
      width: 170,
      height: 23,
    },
  }),
);
